export type TypeUserToView = {
    id: string;
    login: string;
    email: string;
    createdAt: string;
}

export type TypeUser = {
    id: string;
    login: string;
    email: string;
    password: string;
    createdAt: Date;
}
